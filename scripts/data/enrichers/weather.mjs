// Stub. Will fetch climate normals from NOAA NCEI.
// Returns null until the real implementation lands.
export const category = "weather";
export async function fetchForStation(_station, _ctx) {
  return null;
}
